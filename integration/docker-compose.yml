version: '3.1'

services:
  generator:
    image: localhost:9010/gen:ci-10
    # ports:
    #   - "8080:80"
  mail:
    image: mailhog/mailhog
    # ports:
    #   - "8025:8025"
  integration:
    image: localhost:9010/gen:integration-10
    build: .